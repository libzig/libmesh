# orchestrator example

Goal:

1. build a `ResolvedPeer` with direct and relay candidates
2. run route decision logic through `node_orchestrator.connect(...)`
3. demonstrate traversal-aware decision outcomes
4. use driver-backed session opening for direct or relay fallback paths
